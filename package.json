{
  "name": "illuminator",
  "version": "0.1.0",
  "description": "A user-initiated browser extension for discovering IIIF and image resources on the web",
  "scripts": {
    "test": "jest --testEnvironment=jsdom",
    "test:watch": "jest --testEnvironment=jsdom --watch",
    "lint": "eslint src tests"
  },
  "devDependencies": {
    "@babel/core": "^7.29.0",
    "@babel/preset-env": "^7.29.2",
    "babel-jest": "^30.3.0",
    "jest": "^29.0.0",
    "jest-environment-jsdom": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": [
      "**/tests/**/*.test.js"
    ]
  },
  "license": "MIT"
}
